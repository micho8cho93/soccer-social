[phases.setup]
nixPkgs = ['python312', 'postgresql', 'nodejs']

[phases.install]
cmds = [
  'npm ci',
  'pip install -r requirements.txt'
]

[phases.build]
cmds = [
  'npm run build:css',
  'python manage.py collectstatic --no-input --clear'
]

[start]
cmd = 'gunicorn config.wsgi --log-file -'